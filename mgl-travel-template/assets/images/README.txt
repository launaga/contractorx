Drop photography here.

Naming used by the demo markup:
  hero-<slug>.jpg      1920x1080, hero backgrounds
  tour-<slug>.jpg      1200x800, tour cards (3:2)
  team-<slug>.jpg      600x600, team portraits (1:1)
  gallery-<n>.jpg      1200x800, gallery grid

Until real images are added, .card-media renders a gradient with the
data-img label so layouts stay measurable. Replace the gradient with a
background-image (or swap the div for an <img>) once assets land.
